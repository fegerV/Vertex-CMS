<p class="vc-button-wrap">
    <a class="vc-button vc-button-{{ $settings['style'] ?? 'primary' }}" href="{{ $settings['url'] ?? '#' }}" target="{{ $settings['target'] ?? '_self' }}">
        {{ $settings['text'] ?? 'Learn more' }}
    </a>
</p>
